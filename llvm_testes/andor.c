// // 

// void boyd(void){

// 	int a = 1;
// 	int b = 0;
// 	putchar( 'a' + a || a);
// 	putchar( 'a' + a || b);
// 	putchar( 'a' + b || a);
// 	putchar( 'a' + b || b);

// 	putchar( 'a' + a && a);
// 	putchar( 'a' + a && b);
// 	putchar( 'a' + b && a);
// 	putchar( 'a' + b && b);

// 	int c;
// 	c = a || a || a;
// 	putchar( 'a' + c);
// 	c = a || a || b;
// 	putchar( 'a' + c);
// 	c = a || b || a;
// 	putchar( 'a' + c);
// 	c = a || b || b;
// 	putchar( 'a' + c);
// 	c = b || a || a;
// 	putchar( 'a' + c);
// 	c = b || a || b;
// 	putchar( 'a' + c);
// 	c = b || b || a;
// 	putchar( 'a' + c);
// 	c = b || b || b;
// 	putchar( 'a' + c);


// 	putchar( 'a' + c);
// 	c = a && a && a;
// 	putchar( 'a' + c);
// 	c = a && a && b;
// 	putchar( 'a' + c);
// 	c = a && b && a;
// 	putchar( 'a' + c);
// 	c = a && b && b;
// 	putchar( 'a' + c);
// 	c = b && a && a;
// 	putchar( 'a' + c);
// 	c = b && a && b;
// 	putchar( 'a' + c);
// 	c = b && b && a;
// 	putchar( 'a' + c);
// 	c = b && b && b;
// 	putchar( 'a' + c);
// }
int main(void){
	int a = 1;
	int b = 0;
	//putchar( 'a' + a || a);
	//putchar( 'a' + a || b);
	//putchar( 'a' + b || a);
	//putchar( 'a' + b || b);

	putchar(a && a);
// 	putchar( 'a' + a && b);
// 	putchar( 'a' + b && a);
// 	putchar( 'a' + b && b);

// 	int c;
// 	c = a || a || a;
// 	c = a || a || b;
// 	c = a || b || a;
// 	c = a || b || b;
// 	c = b || a || a;
// 	c = b || a || b;
// 	c = b || b || a;
// 	c = b || b || b;


// 	c = a && a && a;
// 	c = a && a && b;
// 	c = a && b && a;
// 	c = a && b && b;
// 	c = b && a && a;
// 	c = b && a && b;
// 	c = b && b && a;
// 	c = b && b && b;
// //	boyd();
// 	putchar('\n');
	return 0;
}