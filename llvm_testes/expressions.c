void sin(int a, short b, char c){
    int d = +(a - b * c * c) / a / b / c * a % b;
    int e = 4;
    int f = d > e < a <= c >= a;
    putchar('a' + d);
    putchar('a' + e);
    putchar('a' + f);
}

int main(void){
    char e = 'a';
    int a = +(-(-(1 + (1 + 1) * 2 % 4 / 3)));
    putchar(e + a);
    sin(1, 2, 3);

}

