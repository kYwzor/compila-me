int t = 1;
char o = 'a';
void sin(int a, short b, char c){
    a + c;
    c + a;
    (c + c) + (c + a);
    /*int d = +(a - b * c * c) / a / b / c * a % b;
    int e = 4;
    int f = !(d > e) < !(a <= c) >= a;
    */
    int g = 1;
    char h = 'a';

    int j = h + g;
    j = h + g;
    int i = a + c;
    i = a + c;
    int l = t + o;
    l = t + o;
    int y = h + o;
    y = h + o;
    j + h + t + y + o + l + i;
}

int main(void){
    char e = 'a';
    int a = +(-(-(1 + (1 + 1) * 2 % 4 / 3)));
    putchar(e + a);
    putchar(!!e);
    sin(1, 2, 3);

}

